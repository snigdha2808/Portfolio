// You can define prop types here if the component needs props in the future
export interface FooterProps {
  // Define any props here if needed
  // For example:
  // additionalText?: string;
  // showSocialLinks?: boolean;
}